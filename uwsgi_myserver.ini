[uwsgi]
base = /root/myserver

home = %(base)/ENV
pythonpath = %(base)
app = main
module = %(app)

socket = /root/myserver/myserver.sock

chmod-socket = 666
callable = app
logto = /root/myserver/%n.log
master = true

